info:
  name: Get Production Analytics
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/productionplans/production-registries/analytics/"
  headers:
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get detailed analytics for production registries
