info:
  name: Get Production Summary
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/productionplans/production-registries/summary/"
  headers:
    - name: Authorization
      value: Bearer {{jwt_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get summary statistics for production registries
