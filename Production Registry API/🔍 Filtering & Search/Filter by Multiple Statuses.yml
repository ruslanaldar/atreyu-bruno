info:
  name: Filter by Multiple Statuses
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/productionplans/production-registries/?status_in=in_process,completed,cancelled"
  headers:
    - name: Authorization
      value: Bearer {{jwt_token}}
  params:
    - name: status_in
      value: in_process,completed,cancelled
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Filter registries by multiple statuses
