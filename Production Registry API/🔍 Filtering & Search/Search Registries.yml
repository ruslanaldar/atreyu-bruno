info:
  name: Search Registries
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/productionplans/production-registries/?search=urgent customer"
  headers:
    - name: Authorization
      value: Bearer {{jwt_token}}
  params:
    - name: search
      value: urgent customer
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Search across multiple fields in registries
