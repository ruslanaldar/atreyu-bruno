info:
  name: List All Inquiries
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/inquiries/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
