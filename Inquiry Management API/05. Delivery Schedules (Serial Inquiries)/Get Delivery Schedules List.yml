info:
  name: Get Delivery Schedules List
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/inquiries/delivery-schedules/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
