info:
  name: "04. Search: max_weight, category, best_for, special_features"
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/inquiries/container-types/?search=bag"
  params:
    - name: search
      value: bag
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
