info:
  name: 03. by Query
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/inquiries/container-types/?id=16"
  params:
    - name: id
      value: "16"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
