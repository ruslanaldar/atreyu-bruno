info:
  name: 02. by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/inquiries/container-types/16"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
