info:
  name: "10. Ordering: name, category, max_weight, created_at"
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/inquiries/container-types/?ordering=-created_at"
  params:
    - name: ordering
      value: -created_at
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
