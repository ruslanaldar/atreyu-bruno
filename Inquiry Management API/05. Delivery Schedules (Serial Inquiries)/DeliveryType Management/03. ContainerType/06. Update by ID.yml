info:
  name: 06. Update by ID
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base_url}}/inquiries/container-types/27"
  body:
    type: json
    data: |-
      {
              "name": "Test Container Type",
              "category": "standard_container",
              "external_dimensions": "20' x 8' x 8.5'",
              "max_weight": 30000,
              "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
