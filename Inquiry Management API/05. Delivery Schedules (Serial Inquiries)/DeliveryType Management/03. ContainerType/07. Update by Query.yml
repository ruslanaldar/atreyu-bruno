info:
  name: 07. Update by Query
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}/inquiries/container-types/?id=27"
  params:
    - name: id
      value: "27"
      type: query
  body:
    type: json
    data: |-
      {
              "name": "Test Container Type",
              "category": "standard_container",
              "external_dimensions": "20' x 8' x 8.5'",
              "max_weight": 35000,
              "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
