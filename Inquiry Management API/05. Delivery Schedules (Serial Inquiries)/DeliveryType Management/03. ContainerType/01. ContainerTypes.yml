info:
  name: 01. ContainerTypes
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/inquiries/container-types/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
