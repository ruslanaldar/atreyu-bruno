info:
  name: 01. Delivery Types
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/inquiries/delivery-types/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
