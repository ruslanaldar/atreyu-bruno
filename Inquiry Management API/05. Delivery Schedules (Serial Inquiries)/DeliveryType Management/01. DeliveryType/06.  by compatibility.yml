info:
  name: 06.  by compatibility
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/inquiries/delivery-types/?action=search_compatible&weight_kg=500&urgency_hours=24&special_requirements=temperature_control,fragile&destination_type=domestic"
  params:
    - name: action
      value: search_compatible
      type: query
    - name: weight_kg
      value: "500"
      type: query
    - name: urgency_hours
      value: "24"
      type: query
    - name: special_requirements
      value: temperature_control,fragile
      type: query
    - name: destination_type
      value: domestic
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
