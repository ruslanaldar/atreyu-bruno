info:
  name: 04.  by Query
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/inquiries/delivery-types/?id=28"
  params:
    - name: id
      value: "28"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
