info:
  name: 03.by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/inquiries/delivery-types/28"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
