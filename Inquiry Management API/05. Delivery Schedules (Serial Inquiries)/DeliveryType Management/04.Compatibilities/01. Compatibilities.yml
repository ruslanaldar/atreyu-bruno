info:
  name: 01. Compatibilities
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/inquiries/compatibilities/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
