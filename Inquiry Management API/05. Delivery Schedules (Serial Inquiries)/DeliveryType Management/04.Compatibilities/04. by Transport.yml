info:
  name: 04. by Transport
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/inquiries/compatibilities/?action=by_transport&transportation_id=1"
  params:
    - name: action
      value: by_transport
      type: query
    - name: transportation_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
