info:
  name: 04. by Container
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/inquiries/compatibilities/?action=by_container&container_id=1"
  params:
    - name: action
      value: by_container
      type: query
    - name: container_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
