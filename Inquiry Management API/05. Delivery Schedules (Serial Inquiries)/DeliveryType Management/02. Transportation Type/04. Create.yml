info:
  name: 04. Create
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/inquiries/transportation-types/"
  body:
    type: json
    data: |
      {
        "name": "Express Delivery",
        "transportation_type": "road_transport",
        "description": "Test Description",
        "typical_delivery_time": "24 hours",
        "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
