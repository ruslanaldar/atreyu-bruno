info:
  name: 08.by ID
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/inquiries/transportation-types/8/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
