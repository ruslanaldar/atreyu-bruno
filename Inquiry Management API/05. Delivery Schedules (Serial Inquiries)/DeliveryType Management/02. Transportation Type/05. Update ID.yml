info:
  name: 05. Update ID
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/inquiries/transportation-types/8/"
  body:
    type: json
    data: |
      {
        "name": "Express Delivery",
        "transportation_type": "road_transport",
        "description": "Test Description",
        "typical_delivery_time": "12 hours",
        "is_active": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
