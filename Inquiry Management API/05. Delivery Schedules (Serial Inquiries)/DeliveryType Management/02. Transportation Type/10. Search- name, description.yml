info:
  name: "10. Search: name, description"
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/inquiries/transportation-types/?search=express"
  params:
    - name: search
      value: express
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
