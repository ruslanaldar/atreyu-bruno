info:
  name: Get Delivery Schedules Inquiry_id
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/inquiries/delivery-schedules/?inquiry_id={{inquiry_id}}"
  params:
    - name: inquiry_id
      value: "{{inquiry_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
