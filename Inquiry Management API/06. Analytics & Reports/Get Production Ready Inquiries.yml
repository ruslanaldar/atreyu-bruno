info:
  name: Get Production Ready Inquiries
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/production-ready/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
