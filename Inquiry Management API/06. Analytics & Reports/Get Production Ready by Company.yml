info:
  name: Get Production Ready by Company
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/production-ready/?company_id={{company_id}}"
  params:
    - name: company_id
      value: "{{company_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
