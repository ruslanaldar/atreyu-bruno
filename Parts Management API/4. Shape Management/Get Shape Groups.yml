info:
  name: Get Shape Groups
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/shape-groups/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
