info:
  name: Get Shape Types
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/shape-types/?group=1"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: group
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
