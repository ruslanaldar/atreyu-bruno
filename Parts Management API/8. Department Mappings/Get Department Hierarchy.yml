info:
  name: Get Department Hierarchy
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/department-file-mappings/department_hierarchy/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
