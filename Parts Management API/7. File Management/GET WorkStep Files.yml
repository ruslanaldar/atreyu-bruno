info:
  name: GET WorkStep Files
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/parts/work-step-files/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
