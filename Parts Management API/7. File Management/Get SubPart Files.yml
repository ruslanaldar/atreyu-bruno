info:
  name: Get SubPart Files
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/sub-part-files/?subpart_id=1639"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: subpart_id
      value: "1639"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
