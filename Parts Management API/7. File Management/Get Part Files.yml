info:
  name: Get Part Files
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/part-files/?part_id=484"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: part_id
      value: "484"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
