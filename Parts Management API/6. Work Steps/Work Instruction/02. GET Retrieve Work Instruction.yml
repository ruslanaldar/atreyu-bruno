info:
  name: GET Retrieve Work Instruction
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/work-steps/271498/work-instruction/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
