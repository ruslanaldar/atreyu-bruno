info:
  name: GET Retrieve Work Instruction by Query Param
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/parts/work-steps/work-instruction/"
  params:
    - name: work_step_id
      value: "271498"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
