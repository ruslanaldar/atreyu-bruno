info:
  name: Delete WorkStep
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/work-steps/delete_by_id/"
  body:
    type: json
    data: |-
      {
          "workstep_id": 351
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
