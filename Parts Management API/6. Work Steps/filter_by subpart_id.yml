info:
  name: filter_by subpart_id
  type: http
  seq: 8

http:
  method: GET
  url: http://27.223.82.66:18080/api/basics/equipments/work-stations/list_with_compatibility/?workcenter_id={{workcenter_id}}&shape_id={{subpart_id}}
  params:
    - name: workcenter_id
      value: "{{workcenter_id}}"
      type: query
    - name: shape_id
      value: "{{subpart_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
