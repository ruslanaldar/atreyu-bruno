info:
  name: work-step-files partID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/part-files/?part_id=123"
  params:
    - name: part_id
      value: "123"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
