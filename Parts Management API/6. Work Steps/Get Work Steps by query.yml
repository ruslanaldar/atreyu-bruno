info:
  name: Get Work Steps by query
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/work-steps/list_by_subpart/?subpart_id=387"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: subpart_id
      value: "387"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
