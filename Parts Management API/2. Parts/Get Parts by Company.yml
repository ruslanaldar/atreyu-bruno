info:
  name: Get Parts by Company
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/company/{{company_id}}/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
