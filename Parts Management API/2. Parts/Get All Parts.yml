info:
  name: Get All Parts
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/parts/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
