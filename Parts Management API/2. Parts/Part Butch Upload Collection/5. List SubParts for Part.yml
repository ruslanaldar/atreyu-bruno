info:
  name: 5. List SubParts for Part
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/sub-parts/?main_part_id={{test_part_id}}"
  params:
    - name: main_part_id
      value: "{{test_part_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all subparts for the main part
