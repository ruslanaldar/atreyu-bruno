info:
  name: 4. Get Batch Upload Template
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/parts/batch_upload_template/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get the template/schema for batch upload
