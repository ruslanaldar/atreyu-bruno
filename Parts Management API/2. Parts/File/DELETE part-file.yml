info:
  name: DELETE part-file
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/parts/part-files/delete/?part_file_id=123"
  params:
    - name: part_file_id
      value: "123"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
