info:
  name: Get Part shapes
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/parts/sub-parts/with-shape/?main_part_id=6930"
  params:
    - name: main_part_id
      value: "6930"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
