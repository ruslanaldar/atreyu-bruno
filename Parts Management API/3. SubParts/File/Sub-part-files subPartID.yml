info:
  name: Sub-part-files subPartID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/sub-part-files/?subpart_id=123"
  params:
    - name: subpart_id
      value: "123"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
