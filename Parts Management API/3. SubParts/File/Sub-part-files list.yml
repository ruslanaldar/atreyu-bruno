info:
  name: Sub-part-files list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/parts/sub-part-files/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
