info:
  name: Get SubPart by Main part ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/parts/sub-parts/?main_part_id=519"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: main_part_id
      value: "519"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
