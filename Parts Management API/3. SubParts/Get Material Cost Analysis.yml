info:
  name: Get Material Cost Analysis
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/sub-parts/{{subpart_id}}/material_cost_analysis/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
