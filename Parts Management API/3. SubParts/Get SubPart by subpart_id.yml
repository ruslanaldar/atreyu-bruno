info:
  name: Get SubPart by subpart_id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/sub-parts/?main_part_id=2498"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: main_part_id
      value: "2498"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
