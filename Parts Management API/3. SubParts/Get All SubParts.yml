info:
  name: Get All SubParts
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/parts/sub-parts/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
