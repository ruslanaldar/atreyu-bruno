info:
  name: Get SubPart Coating by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/coating/subpart-coatings/{{subpart_coating_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get one subpart coating by ID.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/subpart-coatings/{{subpart_coating_id}}/`
  
  Methods accepted by this URL in the API:
  - GET, PUT, PATCH, DELETE
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
