info:
  name: Filter SubPart Coatings by SubPart
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/parts/coating/subpart-coatings/?subpart={{subpart_id}}"
  params:
    - name: subpart
      value: "{{subpart_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  List subpart coatings filtered by subpart id.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/subpart-coatings/?subpart={{subpart_id}}`
  
  Methods accepted by this URL in the API:
  - GET, POST, PUT, PATCH, DELETE
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - This request uses a subpart query parameter for lookup intent.
