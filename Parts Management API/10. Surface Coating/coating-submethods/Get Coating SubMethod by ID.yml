info:
  name: Get Coating SubMethod by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/parts/coating/coating-submethods/{{submethod_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get one coating submethod with color options.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/coating-submethods/{id}/`
  
  Methods accepted by this URL in the API:
  - GET, PUT, PATCH, DELETE
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - Some GET requests in this collection intentionally show body-based lookup by `id` because the current backend accepts merged query/body filters.
