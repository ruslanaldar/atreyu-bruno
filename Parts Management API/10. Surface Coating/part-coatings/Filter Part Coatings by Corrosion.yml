info:
  name: Filter Part Coatings by Corrosion
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/parts/coating/part-coatings/?corrosion_class=C3"
  params:
    - name: corrosion_class
      value: "C3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  List part coatings filtered by corrosion class.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/part-coatings/`
  
  Methods accepted by this URL in the API:
  - GET, POST
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - Some GET requests in this collection intentionally show body-based lookup by `id` because the current backend accepts merged query/body filters.
