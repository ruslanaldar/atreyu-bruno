info:
  name: Get Part Coating by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/parts/coating/part-coatings/{{part_coating_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get one part coating assignment by ID.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/part-coatings/{id}/`
  
  Methods accepted by this URL in the API:
  - GET, PUT, PATCH, DELETE
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - Some GET requests in this collection intentionally show body-based lookup by `id` because the current backend accepts merged query/body filters.
