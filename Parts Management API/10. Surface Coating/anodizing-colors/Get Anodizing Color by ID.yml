info:
  name: Get Anodizing Color by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/parts/coating/anodizing-colors/{{anodizing_color_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get one anodizing color by ID.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/anodizing-colors/{id}/`
  
  Methods accepted by this URL in the API:
  - GET, PUT, PATCH, DELETE
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - Some GET requests in this collection intentionally show body-based lookup by `id` because the current backend accepts merged query/body filters.
