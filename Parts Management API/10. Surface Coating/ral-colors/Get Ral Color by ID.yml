info:
  name: Get Ral Color by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/parts/coating/ral-colors/826bcbe4-466d-41d5-a53f-fba8be6c0747/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Get one RAL color by primary key.

  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/ral-colors/{id}/`

  Methods accepted by this URL in the API:
  - GET

  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - Some GET requests in this collection intentionally show body-based lookup by `id` because the current backend accepts merged query/body filters.
