info:
  name: Filter Ral Colors Classic
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/parts/coating/ral-colors/?ral_type=classic"
  params:
    - name: ral_type
      value: "classic"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  List RAL colors filtered by classic type.
  
  Current Bruno request:
  - Method: GET
  - URL: `{{base_url}}/parts/coating/ral-colors/`
  
  Methods accepted by this URL in the API:
  - GET
  
  Notes:
  - Base API prefix is provided through `{{base_url}}`.
  - Some GET requests in this collection intentionally show body-based lookup by `id` because the current backend accepts merged query/body filters.
