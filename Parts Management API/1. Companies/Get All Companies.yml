info:
  name: Get All Companies
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/companies/"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
