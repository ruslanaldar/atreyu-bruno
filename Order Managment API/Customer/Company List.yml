info:
  name: Company List
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/{{prefix}}/companies/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
