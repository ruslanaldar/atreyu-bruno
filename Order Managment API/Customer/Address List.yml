info:
  name: Address List
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/{{prefix}}/addresses/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
