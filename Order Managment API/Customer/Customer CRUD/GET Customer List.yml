info:
  name: GET Customer List
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/{{prefix}}/customers/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
