info:
  name: Change Address
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/{{prefix}}/addresses/"
  body:
    type: json
    data: |-
      {
        "id":57,
        "company": 61,
        "address_type": "Company Address", //choices: Company Address, Delivery, Invoice Address
        "status": true,
        "address": " TEST 1234 Elm Street, Springfield, IL, 62704, InquiryStatusAPI 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
