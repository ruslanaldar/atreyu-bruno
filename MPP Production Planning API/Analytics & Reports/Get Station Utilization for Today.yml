info:
  name: Get Station Utilization for Today
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/productionplans/station-utilization/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get station utilization for today
