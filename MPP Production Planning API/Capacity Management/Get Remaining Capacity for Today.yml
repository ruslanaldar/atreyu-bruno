info:
  name: Get Remaining Capacity for Today
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/capacity/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get remaining capacity for today
