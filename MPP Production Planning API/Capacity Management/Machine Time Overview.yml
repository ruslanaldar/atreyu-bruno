info:
  name: Machine Time Overview
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/productionplans/machine-time-overview/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
