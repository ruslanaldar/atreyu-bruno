info:
  name: Capacity Analysis
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/productionplans/capacity-analysis-view/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
