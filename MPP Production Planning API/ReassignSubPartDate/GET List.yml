info:
  name: GET List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/reassign-part-date/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
