info:
  name: POST create
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/productionplans/reassign-part-date/"
  body:
    type: json
    data: |
      {
        "container_no": "h-0389",
        "part_no": "92178482",
        "subpart_no": "92178482",
        "reassign_date": "2025-10-30"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
