info:
  name: New Request
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/parts/?container_no=eve-1500"
  params:
    - name: container_no
      value: eve-1500
      type: query
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://27.223.82.66:18080/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
