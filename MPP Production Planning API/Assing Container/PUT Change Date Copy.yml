info:
  name: PUT Change Date Copy
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/productionplans/containers/?id=1"
  params:
    - name: id
      value: "1"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
