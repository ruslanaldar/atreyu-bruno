info:
  name: GET List containers
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/containers/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
