info:
  name: PUT Change Date
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/productionplans/containers/"
  body:
    type: json
    data: |-
      {
          "id": 1,
          "so_etd": "2025-10-22"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
