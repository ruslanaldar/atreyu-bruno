info:
  name: get tickets pagination
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/employee-time-tickets/?emp_id=313&page=1"
  params:
    - name: emp_id
      value: "313"
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
