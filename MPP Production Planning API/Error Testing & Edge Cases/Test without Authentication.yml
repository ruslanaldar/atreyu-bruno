info:
  name: Test without Authentication
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Test authentication requirement (should return 401)
