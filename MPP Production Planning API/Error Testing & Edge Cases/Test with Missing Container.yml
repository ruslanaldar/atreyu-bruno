info:
  name: Test with Missing Container
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/by_container/?container_no=NON_EXISTENT"
  params:
    - name: container_no
      value: NON_EXISTENT
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Test behavior with non-existent container
