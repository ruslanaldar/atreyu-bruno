info:
  name: Test with Invalid Date Format
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/by_date/?date=invalid-date"
  params:
    - name: date
      value: invalid-date
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Test error handling with invalid date format
