info:
  name: Test with Invalid MPP ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/INVALID_ID/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Test error handling with invalid MPP ID
