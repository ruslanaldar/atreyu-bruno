info:
  name: Test with Missing Customer Order
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/by_customer/?mo_no=NON_EXISTENT"
  params:
    - name: mo_no
      value: NON_EXISTENT
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Test behavior with non-existent customer order
