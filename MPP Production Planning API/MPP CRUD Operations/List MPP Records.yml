info:
  name: List MPP Records
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all MPP records with optional filtering and pagination
