info:
  name: last_record
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/latest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
