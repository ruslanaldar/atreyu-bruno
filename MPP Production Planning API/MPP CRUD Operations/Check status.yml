info:
  name: Check status
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/task_status/?task_id=0ff9533b-9215-4cd9-9cae-ae7573e6a2e7"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: task_id
      value: "{{task_id}}"
      type: query
      disabled: true
    - name: task_id
      value: 0ff9533b-9215-4cd9-9cae-ae7573e6a2e7
      type: query
  body:
    type: json
    data: |
      {
          "planning_date": "{{planning_date}}",
          "trigger_reason": "manual_creation",
          "status": "draft"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Create new MPP - automatically runs production planning to generate production_plan and remaining_capacity
