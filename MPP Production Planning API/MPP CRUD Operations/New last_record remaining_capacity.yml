info:
  name: New last_record remaining_capacity
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/latest/?data_type=remaining_capacity"
  params:
    - name: data_type
      value: remaining_capacity
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
