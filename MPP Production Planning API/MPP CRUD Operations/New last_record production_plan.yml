info:
  name: New last_record production_plan
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/latest/?data_type=production_plan"
  params:
    - name: data_type
      value: production_plan
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
