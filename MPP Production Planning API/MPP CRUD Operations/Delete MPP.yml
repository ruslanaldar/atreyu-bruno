info:
  name: Delete MPP
  type: http
  seq: 16

http:
  method: DELETE
  url: "{{base_url}}/productionplans/mpp/{{mpp_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Delete an MPP record
