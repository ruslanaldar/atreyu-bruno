info:
  name: GET AssignedPart
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/assigned-parts/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
