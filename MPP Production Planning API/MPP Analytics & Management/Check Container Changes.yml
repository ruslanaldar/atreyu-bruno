info:
  name: Check Container Changes
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/check_containers/?mpp_id={{mpp_id}}"
  params:
    - name: mpp_id
      value: "{{mpp_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Check for container changes in current planning vs specified MPP
