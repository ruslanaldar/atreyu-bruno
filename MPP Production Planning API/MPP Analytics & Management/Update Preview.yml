info:
  name: Update Preview
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/{{mpp_id}}/update_preview/?status=confirmed&notes=test"
  params:
    - name: status
      value: confirmed
      type: query
    - name: notes
      value: test
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Preview what would happen with an update without actually doing it
