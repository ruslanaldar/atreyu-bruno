info:
  name: Get RED Tickets
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/productionplans/part-tickets/red/"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
