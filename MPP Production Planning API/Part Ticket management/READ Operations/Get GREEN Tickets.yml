info:
  name: Get GREEN Tickets
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/api/productionplans/part-tickets/green/"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
