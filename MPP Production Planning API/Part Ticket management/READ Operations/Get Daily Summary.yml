info:
  name: Get Daily Summary
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/productionplans/part-tickets/daily-summary/"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
