info:
  name: Get YELLOW Tickets
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/productionplans/part-tickets/yellow/"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
