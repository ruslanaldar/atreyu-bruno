info:
  name: 1. Preview update impact
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/{{mpp_id}}/update_preview/?status=confirmed"
  params:
    - name: status
      value: confirmed
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Step 1: Preview update impact"
