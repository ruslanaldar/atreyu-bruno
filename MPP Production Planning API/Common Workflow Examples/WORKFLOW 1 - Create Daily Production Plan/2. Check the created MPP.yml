info:
  name: 2. Check the created MPP
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/mpp/latest/?date={{planning_date}}"
  params:
    - name: date
      value: "{{planning_date}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Step 2: Verify created MPP"
