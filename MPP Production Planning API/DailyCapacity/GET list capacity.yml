info:
  name: GET list capacity
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/daily-capacity/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
