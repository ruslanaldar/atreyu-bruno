info:
  name: GET gantt all containers
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers/?container_no=sny39"
  params:
    - name: container_no
      value: sny39
      type: query
    - name: scenario_id
      value: no_limit
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
