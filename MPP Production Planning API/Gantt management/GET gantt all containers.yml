info:
  name: GET gantt all containers
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers/?container_no=sny38&scenario_id=no_limit"
  params:
    - name: container_no
      value: sny38
      type: query
    - name: scenario_id
      value: no_limit
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
