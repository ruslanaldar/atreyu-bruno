info:
  name: GET gantt workcenters analytics
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/workcenter_analysis"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
