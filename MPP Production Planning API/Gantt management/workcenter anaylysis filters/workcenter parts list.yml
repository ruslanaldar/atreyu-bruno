info:
  name: workcenter parts list
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenter-parts/?wc_code=w04"
  params:
    - name: wc_code
      value: w04
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
