info:
  name: GET gantt workcenters analytics wc_code
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/workcenter_analysis/?wc_code=w04"
  params:
    - name: wc_code
      value: w04
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
