info:
  name: GET gantt all containers Get ProductionPlan
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers_overview/?is_production=true"
  params:
    - name: is_production
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
