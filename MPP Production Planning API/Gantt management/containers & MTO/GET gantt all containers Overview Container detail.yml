info:
  name: GET gantt all containers Overview Container detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers/?container_no=sny38"
  params:
    - name: container_no
      value: sny38
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
