info:
  name: GET gantt all containers Overview Report Scenario
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers_overview/?get_report=true&scenario_id=recommended-8"
  params:
    - name: get_report
      value: "true"
      type: query
    - name: scenario_id
      value: recommended-8
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
