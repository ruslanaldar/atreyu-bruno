info:
  name: GET gantt all containers Scenario timeticket base plan
  type: http
  seq: 21

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers_overview/?emp_tickets=true&ticket_date=2026-04-07"
  params:
    - name: emp_tickets
      value: "true"
      type: query
    - name: ticket_date
      value: 2026-04-07
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
