info:
  name: POST capacity-extended recommended-8
  type: http
  seq: 20

http:
  method: POST
  url: "{{base_url}}/productionplans/capacity-extended/"
  body:
    type: json
    data: |-
      {  
        "scenario_id": "we_recomend",
        "recommended_id": "recommended-8",
        "extra_stations": [
          {
            "wc_code": "l02",
            "quantity": 1,
            "days" : 1
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
