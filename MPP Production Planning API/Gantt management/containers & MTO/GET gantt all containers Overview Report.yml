info:
  name: GET gantt all containers Overview Report
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers_overview/?get_report=true"
  params:
    - name: get_report
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
