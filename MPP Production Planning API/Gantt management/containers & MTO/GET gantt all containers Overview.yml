info:
  name: GET gantt all containers Overview
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers_overview/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
