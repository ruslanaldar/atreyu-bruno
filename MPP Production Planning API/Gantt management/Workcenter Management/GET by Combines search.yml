info:
  name: GET by Combines search
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenters/"
  body:
    type: json
    data: |-
      {
          "container_no": "wk-0002",
          "employee_name": "Huang Zelun",
          "part_no": "60001927"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
