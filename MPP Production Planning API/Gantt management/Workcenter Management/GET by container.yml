info:
  name: GET by container
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenters/"
  body:
    type: json
    data: |-
      {
          "container_no": "wk-0002"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
