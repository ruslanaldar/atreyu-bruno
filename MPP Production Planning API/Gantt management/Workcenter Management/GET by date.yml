info:
  name: GET by date
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenters/"
  body:
    type: json
    data: |-
      {
          "center_code": "l01",
          "date": "2025-12-10"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
