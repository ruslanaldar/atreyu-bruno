info:
  name: GET Workcenter list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenters/"
  auth: inherit

runtime:
  variables:
    - name: base_url
      value: http://27.223.82.66:18080/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
