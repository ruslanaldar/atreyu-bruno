info:
  name: GET by WorkCenter
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenters/"
  body:
    type: json
    data: |-
      {
          "center_code": "l01"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
