info:
  name: GET by Main Part
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/productionplans/workcenters/"
  body:
    type: json
    data: |-
      {
          "part_no": "60001929"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
