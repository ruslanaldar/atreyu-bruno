info:
  name: Rebuild cache
  type: http
  seq: 10

http:
  method: POST
  url: "{{base_url}}/productionplans/gantt/rebuild_cache/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
