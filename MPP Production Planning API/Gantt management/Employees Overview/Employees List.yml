info:
  name: Employees List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/employees/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
