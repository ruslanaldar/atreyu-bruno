info:
  name: Employees List SCENARIO
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/productionplans/employees?scenario_id=recommended"
  params:
    - name: scenario_id
      value: recommended
      type: query
    - name: scenario_id
      value: recommended
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
