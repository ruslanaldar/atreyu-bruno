info:
  name: GET by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/employees/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
