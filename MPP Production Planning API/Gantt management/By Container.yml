info:
  name: By Container
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers/?container_no=s354"
  params:
    - name: container_no
      value: s354
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
