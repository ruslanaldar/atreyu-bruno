info:
  name: By Container
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/containers/?container_no=eve-1500-2"
  params:
    - name: container_no
      value: eve-1500-2
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
