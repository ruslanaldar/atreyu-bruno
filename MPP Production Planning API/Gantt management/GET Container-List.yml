info:
  name: GET Container-List
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/productionplans/gantt/container_list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
