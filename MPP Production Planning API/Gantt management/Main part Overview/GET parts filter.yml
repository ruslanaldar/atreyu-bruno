info:
  name: GET parts filter
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/productionplans/parts/"
  body:
    type: json
    data: |-
      {
          "container_no": "cl-0144",
          "part_id": 611
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
