info:
  name: GET parts list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/parts/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
