info:
  name: GET List ReassignWorkStep
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/productionplans/reassign-worksteps/"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
