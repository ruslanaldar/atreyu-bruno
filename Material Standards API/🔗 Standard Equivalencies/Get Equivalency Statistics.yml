info:
  name: Get Equivalency Statistics
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/standard-equivalencies/statistics/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get statistics about equivalencies
