info:
  name: List Standard Equivalencies
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/standard-equivalencies/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all standard equivalencies
