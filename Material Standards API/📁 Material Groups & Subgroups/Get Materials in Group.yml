info:
  name: Get Materials in Group
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/material-groups/13/materials/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all materials in a specific group
