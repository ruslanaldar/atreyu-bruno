info:
  name: Get Standards in Subgroup
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/material-subgroups/13/standards/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all standards in a specific subgroup
