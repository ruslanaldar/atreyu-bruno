info:
  name: Get Material Hierarchy
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/material-groups/hierarchy/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get complete material hierarchy
