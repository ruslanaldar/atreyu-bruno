info:
  name: Get Material Group with Subgroups
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/material-groups/13/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get detailed group info with subgroups
