info:
  name: List Material Groups
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/material-groups/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all material groups
