info:
  name: Test SubPart Exists
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/parts/sub-parts/check_record?subpart_id=8"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  params:
    - name: subpart_id
      value: "8"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Check if SubPart exists and get available operations
