info:
  name: Get Equivalent Standards
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/material-standards/98/equivalents/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all equivalent standards for a specific standard
