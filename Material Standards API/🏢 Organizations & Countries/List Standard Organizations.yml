info:
  name: List Standard Organizations
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/basics/materials/standard-organizations/"
  headers:
    - name: Authorization
      value: Bearer {{authToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all standard organizations
