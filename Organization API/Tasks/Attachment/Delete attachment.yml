info:
  name: Delete attachment
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}{{prefix}}nested/"
  body:
    type: json
    data: |-
      {
        "action": "delete_attachment",
        "taskId": 3,
        "attachmentId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
