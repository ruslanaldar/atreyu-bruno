info:
  name: List Comments
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}{{prefix}}nested/?action=list_comments&taskId=3"
  params:
    - name: action
      value: list_comments
      type: query
    - name: taskId
      value: "3"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
