info:
  name: Single comment
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}{{prefix}}nested/?action=get_comment&taskId=3&commentId=1"
  params:
    - name: action
      value: get_comment
      type: query
    - name: taskId
      value: "3"
      type: query
    - name: commentId
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
