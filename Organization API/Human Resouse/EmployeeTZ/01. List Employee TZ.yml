info:
  name: List employee TZ
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/employee-tz/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
