info:
  name: Details
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/details/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
