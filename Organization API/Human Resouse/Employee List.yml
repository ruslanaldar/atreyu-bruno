info:
  name: Employee List
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/custom-users/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
