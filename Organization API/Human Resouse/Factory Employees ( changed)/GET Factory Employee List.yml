info:
  name: GET Factory Employee List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/factory/employees/"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
