info:
  name: Sync Employee Org Group
  type: http
  seq: 13

http:
  method: POST
  url: "{{base_url}}/organization/categories/hr/employee-access/{{employee_id}}/sync-group/"
  body:
    type: multipart-form
    data:
      - name: clear_existing_org_groups
        type: text
        value: "true"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
