info:
  name: list users in group
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/auth-groups/{{group_id}}/users/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
