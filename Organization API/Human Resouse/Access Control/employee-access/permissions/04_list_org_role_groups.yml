info:
  name: List org role groups
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/employee-access/groups/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
