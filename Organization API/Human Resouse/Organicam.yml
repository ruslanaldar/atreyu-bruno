info:
  name: Organicam
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/organicams/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
