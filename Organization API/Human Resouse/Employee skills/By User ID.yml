info:
  name: By User ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/employee-skills/by_user/?user_id=207"
  params:
    - name: user_id
      value: "207"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
