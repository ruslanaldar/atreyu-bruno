info:
  name: BY ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/organization/categories/hr/employee-skills/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
