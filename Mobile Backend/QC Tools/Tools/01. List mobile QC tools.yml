info:
  name: List mobile QC tools
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/mobile/qc/tools/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List QC tools through the mobile backend route.
