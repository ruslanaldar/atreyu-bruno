info:
  name: Delete mobile QC tool
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/mobile/qc/tools/{{mobile_qc_tool_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the mobile-created QC tool before deleting its mobile-created category.
