info:
  name: Get mobile QC tool by query ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/mobile/qc/tools/?id={{mobile_qc_tool_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the mobile-created QC tool through the mobile ?id= query parameter.
