info:
  name: Delete mobile QC tool category
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/mobile/qc/tools/categories/{{mobile_qc_category_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the mobile-created QC-tool category after its mobile-created tools are deleted.
