info:
  name: Get mobile QC tool category by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/mobile/qc/tools/categories/{{mobile_qc_category_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the mobile-created QC-tool category through the detail route.
