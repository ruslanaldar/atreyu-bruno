info:
  name: List mobile QC tool categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/mobile/qc/tools/categories/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List QC-tool categories through the mobile backend route.
