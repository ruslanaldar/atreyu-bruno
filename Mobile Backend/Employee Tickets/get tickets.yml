info:
  name: get tickets
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/employee-time-tickets/313/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
