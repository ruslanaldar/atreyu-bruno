info:
  name: List QC tickets by status red
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-tickets/by-status/?status=red"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Description: List red tickets through the status filter endpoint.
