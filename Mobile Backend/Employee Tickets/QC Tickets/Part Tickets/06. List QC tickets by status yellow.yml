info:
  name: List QC tickets by status yellow
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-tickets/by-status/?status=yellow"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Description: List yellow tickets through the status filter endpoint.
