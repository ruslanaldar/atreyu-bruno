info:
  name: List QC tickets by container
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-tickets/?container_no=wk-0002"
  params:
    - name: container_no
      value: wk-0002
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Description: List QC tickets for a container.
  Required query parameter: container_no.
