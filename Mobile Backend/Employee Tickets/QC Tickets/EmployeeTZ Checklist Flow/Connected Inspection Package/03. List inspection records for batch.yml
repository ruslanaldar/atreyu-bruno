info:
  name: List inspection records for batch
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-control-plan/inspection-records/?batch={{inspection_batch_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List inspection records submitted for the current inspection batch.
