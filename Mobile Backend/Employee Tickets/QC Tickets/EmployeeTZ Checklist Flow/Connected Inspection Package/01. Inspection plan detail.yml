info:
  name: Inspection plan detail
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-control-plan/inspection-plans/{{inspection_plan_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Shows the inspection plan used to calculate and drive the connected inspection batch.
