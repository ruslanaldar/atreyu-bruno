info:
  name: Notifications
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/chat/notifications/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fetch notifications, including QC notification payloads.
