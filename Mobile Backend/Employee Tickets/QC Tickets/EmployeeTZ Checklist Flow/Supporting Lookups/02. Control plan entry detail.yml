info:
  name: Control plan entry detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-control-plan/entries/{{control_plan_entry_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fetch the mobile Control Plan entry detail connected to the checklist.
