info:
  name: FMEA entry detail
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-fmea/entries/{{fmea_entry_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fetch the mobile FMEA entry detail connected to the checklist.
