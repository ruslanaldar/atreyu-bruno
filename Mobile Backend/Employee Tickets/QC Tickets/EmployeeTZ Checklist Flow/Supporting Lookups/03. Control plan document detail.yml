info:
  name: Control plan document detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-control-plan/documents/{{control_plan_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fetch the mobile Control Plan document detail connected to the checklist.
