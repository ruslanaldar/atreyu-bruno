info:
  name: Connected inspection batch detail
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-control-plan/inspection-batches/{{mobile_inspection_batch_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fetch the inspection batch connected to the mobile checklist.
