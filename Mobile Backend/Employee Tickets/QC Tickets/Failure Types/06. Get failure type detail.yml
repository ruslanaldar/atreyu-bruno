info:
  name: Get failure type detail
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-tickets/failure-types/{{crud_child_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retrieve one failure type node, including parent_id and parent_name.
