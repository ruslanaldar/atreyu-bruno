info:
  name: Delete main failure type
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base_url}}/mobile/timetickets/qc-tickets/failure-types/{{crud_main_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the temporary main failure type after deleting its child nodes.
