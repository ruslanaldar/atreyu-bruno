info:
  name: List employee TZ
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/employee-tz/?page=1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
