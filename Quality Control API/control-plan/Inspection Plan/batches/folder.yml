info:
  name: batches
  type: folder
  seq: 5

request:
  auth: inherit

docs:
  content: |
    Inspection Batch requests. Batch creation calculates sample size from the stricter of RPN inspection rate and AQL sample table, generates immutable sample_positions, and opens the batch.
  type: text/markdown
