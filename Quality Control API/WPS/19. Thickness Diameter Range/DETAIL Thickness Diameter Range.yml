info:
  name: DETAIL Thickness Diameter Range
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/thickness-diameter-ranges/?id={{thickness_range_id}}"
  params:
    - name: id
      value: "{{thickness_range_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
