info:
  name: GET wps check welder
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/basics/wps/check-welder/?id={{wps_id}}&employee_id=1"
  params:
    - name: id
      value: "{{wps_id}}"
      type: query
    - name: employee_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Request: Check Welder Against WPS
  Method: GET
  URL: {{base_url}}/api/basics/wps/check-welder/?id={{wps_id}}&employee_id=1

  Purpose:
  Checks whether a specific employee has an active qualification matching the WPS variables.

  Important:
  This endpoint returns a direct JSON object, not the usual success/message/data envelope.
