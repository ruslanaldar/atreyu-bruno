info:
  name: DETAIL Current Type by query set
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/current-types/?id={{current_type_id}}"
  params:
    - name: id
      value: "{{current_type_id}}"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
