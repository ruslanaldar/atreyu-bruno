info:
  name: DETAIL Current Type by ID NOT WORKING
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/wps/current-types/{{current_type_id}}/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
