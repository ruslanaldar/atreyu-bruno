info:
  name: List Standard Families
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/wps/standard-families/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
