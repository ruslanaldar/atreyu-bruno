info:
  name: PATCH Standard Family detail by id query params update
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/basics/wps/standard-families/?id={{standard_family_id}}"
  params:
    - name: id
      value: "{{standard_family_id}}"
      type: query
  body:
    type: json
    data: |-
      {
        "description": "Patched description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
