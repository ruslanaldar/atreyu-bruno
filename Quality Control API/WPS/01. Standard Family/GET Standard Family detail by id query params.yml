info:
  name: GET Standard Family detail by id query params
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/wps/standard-families/?id={{standard_family_id}}"
  params:
    - name: id
      value: "{{standard_family_id}}"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
