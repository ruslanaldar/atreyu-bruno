info:
  name: GET Standard Family detail by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/standard-families/{{standard_family_id}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
