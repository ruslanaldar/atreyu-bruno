info:
  name: FILTER wps
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/wps/?is_pwps=false"
  params:
    - name: is_pwps
      value: "false"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
