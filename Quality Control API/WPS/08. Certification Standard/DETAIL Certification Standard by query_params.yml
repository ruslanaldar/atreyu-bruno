info:
  name: DETAIL Certification Standard by query_params
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/basics/wps/standards/?id={{standard_id}}"
  params:
    - name: id
      value: "{{standard_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
