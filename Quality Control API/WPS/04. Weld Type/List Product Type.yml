info:
  name: List Product Type
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/wps/weld-types/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
