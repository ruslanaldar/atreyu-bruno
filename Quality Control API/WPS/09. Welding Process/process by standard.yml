info:
  name: process by standard
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/wps/welding-processes/?standard=TESTSTD-2"
  params:
    - name: standard
      value: TESTSTD-2
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
