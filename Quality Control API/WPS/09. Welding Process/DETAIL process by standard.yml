info:
  name: DETAIL process by standard
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/welding-processes/?id={{welding_process_id}}"
  params:
    - name: id
      value: "{{welding_process_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
