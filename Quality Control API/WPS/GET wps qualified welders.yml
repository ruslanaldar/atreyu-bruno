info:
  name: GET wps qualified welders
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/basics/wps/qualified-welders/?id={{wps_id}}"
  params:
    - name: id
      value: "{{wps_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Request: Get Qualified Welders For WPS
  Method: GET
  URL: {{base_url}}/api/basics/wps/qualified-welders/?id={{wps_id}}

  Purpose:
  Returns active welder qualifications that cover the selected WPS variables.

  Required Query Param:
  id = WPS id

  Success Response:
  {
    "success": true,
    "message": "Qualified welders for WPS ... retrieved successfully.",
    "data": []
  }
