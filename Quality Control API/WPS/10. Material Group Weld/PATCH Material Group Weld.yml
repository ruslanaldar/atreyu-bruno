info:
  name: PATCH Material Group Weld
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/wps/material-group-welds/{{material_group_weld_id}}/"
  body:
    type: json
    data: |-
      {
        "description": "Patched material group description"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
