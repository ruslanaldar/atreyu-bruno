info:
  name: DETAIL Filler Material Classification
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/filler-material-classifications/?id={{filler_classification_id}}"
  params:
    - name: id
      value: "{{filler_classification_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
