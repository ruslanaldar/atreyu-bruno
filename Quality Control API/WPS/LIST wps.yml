info:
  name: LIST wps
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}/basics/wps/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
