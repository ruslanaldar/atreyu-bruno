info:
  name: DELETE Validity Rule
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/basics/wps/validity-rules/{{validity_rule_id}}/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
