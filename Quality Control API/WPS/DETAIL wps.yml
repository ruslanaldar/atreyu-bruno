info:
  name: DETAIL wps
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/?id={{wps_id}}"
  params:
    - name: id
      value: "{{wps_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Request: Get WPS Detail
  Method: GET
  URL: {{base_url}}/api/basics/wps/{{wps_id}}/

  Alternative:
  GET {{base_url}}/api/basics/wps/?id={{wps_id}}

  Purpose:
  Returns one WPS record with nested runs, weld details, work_step_detail,
  designation, and qualified_welder_count.

  Success Response:
  {
    "success": true,
    "message": "WPS retrieved successfully.",
    "data": {...}
  }
