info:
  name: UPDATE filler material
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/basics/wps/filler-material-types/?id={{filler_material_id}}"
  params:
    - name: id
      value: "{{filler_material_id}}"
      type: query
  body:
    type: json
    data: |-
      {
        "description":"Patched filler material"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
