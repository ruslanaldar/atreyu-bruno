info:
  name: DETAIL filler material
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/filler-material-types/?id={{filler_material_id}}"
  params:
    - name: id
      value: "{{filler_material_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
