info:
  name: DELETE wps
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/basics/wps/?id={{wps_id}}"
  params:
    - name: id
      value: "{{wps_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Request: Delete WPS Record
  Method: DELETE
  URL: {{base_url}}/api/basics/wps/{{wps_id}}/

  Alternative:
  DELETE {{base_url}}/api/basics/wps/?id={{wps_id}}

  Purpose:
  Deletes one WPS record.

  Success Response:
  {
    "success": true,
    "message": "WPS deleted successfully.",
    "data": null
  }
