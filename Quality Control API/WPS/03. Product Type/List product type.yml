info:
  name: List product type
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/wps/product-types/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
