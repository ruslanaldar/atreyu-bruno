info:
  name: Detail product Type
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/product-types/{{product_type_id}}/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
