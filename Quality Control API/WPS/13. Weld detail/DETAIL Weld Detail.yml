info:
  name: DETAIL Weld Detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/weld-details/?id={{weld_detail_id}}"
  params:
    - name: id
      value: "{{weld_detail_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
