info:
  name: GET Designation Format Detail by id query param
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/wps/designation-formats/?id={{designation_format_id}}"
  params:
    - name: id
      value: "{{designation_format_id}}"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
