info:
  name: GET Designation Format Detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/designation-formats/{{designation_format_id}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
