info:
  name: List Designation Format
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/wps/designation-formats/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
