info:
  name: DETAIL Inspection Method
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/inspection-methods/?id={{inspection_method_id}}"
  params:
    - name: id
      value: "{{inspection_method_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
