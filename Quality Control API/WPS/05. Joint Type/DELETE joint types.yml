info:
  name: DELETE joint types
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/basics/wps/joint-types/{{joint_type_id}}/"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
