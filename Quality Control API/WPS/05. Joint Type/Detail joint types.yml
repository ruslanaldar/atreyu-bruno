info:
  name: Detail joint types
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/joint-types/{{joint_type_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
