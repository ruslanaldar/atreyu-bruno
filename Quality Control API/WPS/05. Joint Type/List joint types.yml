info:
  name: List joint types
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/wps/joint-types/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
