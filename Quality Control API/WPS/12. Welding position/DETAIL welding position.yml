info:
  name: DETAIL welding position
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/welding-positions/?id={{welding_position_id}}"
  params:
    - name: id
      value: "{{welding_position_id}}"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
