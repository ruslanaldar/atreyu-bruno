info:
  name: PATCH Groove Type
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base_url}}/basics/wps/groove-types/{{groove_type_id}}/"
  body:
    type: multipart-form
    data:
      - name: description
        type: text
        value: Patched groove type description
      - name: image
        type: file
        value:
          - Parts Management API/6. Work Steps/image1.jpg
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Request: Patch Groove Type
  Method: PATCH
  URL: {{base_url}}/api/basics/wps/groove-types/{{groove_type_id}}/

  Body Type:
  multipart-form

  Purpose:
  Updates text fields and can replace the groove diagram image.
