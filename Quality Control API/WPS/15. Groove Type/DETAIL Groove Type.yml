info:
  name: DETAIL Groove Type
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/groove-types/?id={{groove_type_id}}"
  params:
    - name: id
      value: "{{groove_type_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
