info:
  name: FILTER Welder Qualification
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/wps/welder-qualifications/?status=active"
  params:
    - name: status
      value: active
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
