info:
  name: DETAIL Welder Qualification
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/basics/wps/welder-qualifications/?id={{welder_qualification_id}}"
  params:
    - name: id
      value: "{{welder_qualification_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
