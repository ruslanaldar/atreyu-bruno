info:
  name: Get QC tool category by query ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/qcs/qc-tools/categories/?id={{qc_category_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the captured QC tool category using the supported ?id= query parameter.
