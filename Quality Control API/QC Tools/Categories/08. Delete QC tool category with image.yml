info:
  name: Delete QC tool category with image
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/qcs/qc-tools/categories/{{qc_category_image_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the QC tool category created by the multipart image-upload request.
