info:
  name: Delete QC tool category
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/qcs/qc-tools/categories/{{qc_category_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the captured QC tool category after linked QC tools have been deleted.
