info:
  name: List QC tool categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/qcs/qc-tools/categories/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all existing QC tool categories.
