info:
  name: Get QC tool category by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/qcs/qc-tools/categories/{{qc_category_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the captured QC tool category through its REST detail URL.
