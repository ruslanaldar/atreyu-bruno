info:
  name: Get QC tool by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/qcs/qc-tools/{{qc_tool_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the captured QC tool through its REST detail URL.
