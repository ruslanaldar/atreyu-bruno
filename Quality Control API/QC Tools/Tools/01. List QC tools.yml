info:
  name: List QC tools
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/qcs/qc-tools/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all QC tools.
