info:
  name: Get QC tool by query ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/qcs/qc-tools/?id={{qc_tool_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the captured QC tool using the supported ?id= query parameter.
