info:
  name: List green tickets by EmployeeTZ UUID
  type: http
  seq: 12

http:
  method: GET
  url: "{{base_url}}/mobile/timetickets/qc-tickets/green/?employee_tz={{employee_tz_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List GREEN QC PartTickets linked to the real EmployeeTZ UUID.
