info:
  name: QC task detail
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/organization/tasks/?action=detail&taskId={{task_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Fetch the QC task detail created from EmployeeTZ/checklist generation.
