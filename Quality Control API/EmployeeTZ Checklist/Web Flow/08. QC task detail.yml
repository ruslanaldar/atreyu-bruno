info:
  name: QC task detail
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/organization/tasks/?action=detail&taskId=76"
  params:
    - name: action
      value: detail
      type: query
    - name: taskId
      value: "76"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Fetch the QC task detail created from EmployeeTZ/checklist generation.
