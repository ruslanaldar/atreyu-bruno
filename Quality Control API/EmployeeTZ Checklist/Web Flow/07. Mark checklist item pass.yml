info:
  name: Mark checklist item pass
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{base_url}}/qcs/employee-tz/checklist-items/{{checklist_item_id}}/"
  body:
    type: json
    data: |-
      {
        "status": "pass",
        "notes": "Checked and accepted"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Mark one connected checklist item as pass.
