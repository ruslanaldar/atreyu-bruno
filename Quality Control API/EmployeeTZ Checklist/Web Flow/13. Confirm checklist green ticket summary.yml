info:
  name: Confirm checklist green ticket summary
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/qcs/employee-tz/checklists/{{checklist_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Re-fetch checklist detail after GREEN ticket creation to confirm green ticket summary and green_tickets list.
