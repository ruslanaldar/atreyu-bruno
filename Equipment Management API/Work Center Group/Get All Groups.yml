info:
  name: Get All Groups
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/equipments/work-center-groups/?page=1"
  params:
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
