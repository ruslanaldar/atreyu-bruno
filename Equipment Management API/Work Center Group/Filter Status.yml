info:
  name: Filter Status
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/{{api_prefix}}/work-center-groups/?status=active&ordering=name"
  params:
    - name: status
      value: active
      type: query
    - name: ordering
      value: name
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
