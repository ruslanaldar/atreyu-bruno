info:
  name: Delete Group
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/{{api_prefix}}/work-center-groups/?group_id={{created_group_id}}"
  params:
    - name: group_id
      value: "{{created_group_id}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
