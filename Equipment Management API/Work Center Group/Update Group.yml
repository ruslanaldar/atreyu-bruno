info:
  name: Update Group
  type: http
  seq: 6

http:
  method: PUT
  url: "{{base_url}}/{{api_prefix}}/work-center-groups/?group_id={{group_id}}"
  params:
    - name: group_id
      value: "{{group_id}}"
      type: query
  body:
    type: json
    data: |-
      {
          "name": "Updated Assembly Department",
          "name_cn": "更新的装配部门"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
