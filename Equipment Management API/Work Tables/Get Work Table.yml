info:
  name: Get Work Table
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/{{api_prefix}}/work-tables/525562b9-085b-467a-b260-f017251df422"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
