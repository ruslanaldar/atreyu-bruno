info:
  name: Patch basic tool
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/basics/tools/{{basic_tool_id}}/"
  body:
    type: json
    data: |-
      {
        "current_qty": 3,
        "description": "Updated by the Bruno CRUD collection."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Partially update the captured basic tool inventory quantity and description.
