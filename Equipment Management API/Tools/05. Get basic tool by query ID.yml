info:
  name: Get basic tool by query ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/basics/tools/?id={{basic_tool_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the captured basic tool using the supported ?id= query parameter.
