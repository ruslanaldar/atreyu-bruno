info:
  name: Delete basic tool
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base_url}}/basics/tools/{{basic_tool_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the captured basic tool.
