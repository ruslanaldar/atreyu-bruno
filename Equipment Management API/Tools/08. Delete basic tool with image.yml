info:
  name: Delete basic tool with image
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/basics/tools/{{basic_tool_image_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete the basic tool created by the multipart image-upload request.
