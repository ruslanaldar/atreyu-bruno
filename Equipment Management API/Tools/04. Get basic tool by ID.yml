info:
  name: Get basic tool by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/basics/tools/{{basic_tool_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Read the captured basic tool through its REST detail URL.
