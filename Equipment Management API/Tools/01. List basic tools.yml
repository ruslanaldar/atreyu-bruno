info:
  name: List basic tools
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/basics/tools/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all basic tools.
